# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [voicepad-v0.1.5] - 2026-05-03

### Features

- **config,transcription**: Add dynamic model discovery and configurable cache paths
- **tui,transcription,cli**: Add download progress callback and redesign setup wizard
- **cli,tui**: Include model name in markdown transcription metadata
- **tui**: Add delete confirmation modal and tab-aware keyboard bindings
- **tui**: Auto-select latest history entry and refine action bindings
- **tui**: Remove retranscribe button from history tab
- **tui,config**: Add system-wide hotkey and status overlay
- **tui**: Defer hotkey listener startup until model is ready
- **tui**: Redesign status overlay with light theme and rounded pill shape
- **tui**: Replace hotkey text input with interactive picker UI
- **tui**: Add VoiceButton component for consistent styling across all interactive buttons
- **tui**: Add VoiceCheckbox component with minimal chrome-free styling
- **tui**: Update default button colors for improved visual hierarchy
- Add theme configuration support with UI selector
- **hotkey-support**: Update checkbox symbols and remove manual button text handling
- **tui**: Apply theme changes immediately without restart
- **overlay**: Add multi-theme support to status overlay

### Bug Fixes

- **tui**: Add type safety and improve callback handling
- **config**: Expand tilde in default paths and add comprehensive path validation
- **tui**: Use Config class model_fields instead of instance to avoid Pydantic v3 deprecation
- **tui**: Improve progress feedback and status message handling
- **tui**: Improve model download progress tracking and UX

### Refactor

- Tests for TUI modules and enhance coverage
- **tui**: Extract components, services, and utilities into modular structure
- **tui**: Remove backward-compatibility shim for markdown utilities
- **tui**: Extract handlers and managers into modular architecture
- **tui**: Move worker methods from history handler to app level
- **hotkey-support**: Remove segments section from markdown output
- **tui**: Simplify theme management and use built-in Textual themes
- **tui**: Move theme configuration to separate TUI config layer

### Testing

- **tui**: Add comprehensive unit tests for refactored components and utilities
- **history-handler**: Use patch.object for mocking retranscribe_file
- **recording-handler**: Update stop_recording test to mock app._finalize_worker
- **ci**: Add display test marker and skip GPU tests by default

<!-- generated by git-cliff -->
